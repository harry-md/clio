info:
  name: Delete Category
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/categories/26
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
