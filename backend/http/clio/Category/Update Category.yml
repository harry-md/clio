info:
  name: Update Category
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{LOCAL_API_URL}}/categories/26"
  body:
    type: json
    data: |-
      {
        "name": "testafasdfadsfsfa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
