info:
  name: Get Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_API_URL}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
