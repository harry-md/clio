info:
  name: Create Category
  type: http
  seq: 3

http:
  method: POST
  url: "{{LOCAL_API_URL}}/categories"
  body:
    type: json
    data: |-
      {
        "name": "test test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
