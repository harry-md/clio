info:
  name: Create Category
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/categories
  body:
    type: json
    data: |-
      {
        "name": "test test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
