info:
  name: Get Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{LOCAL_API_URL}}/current-user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
