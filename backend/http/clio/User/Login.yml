info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCAL_API_URL}}/api/login"
  body:
    type: json
    data: |-
      {
        "username": "pub1",
        "password": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
