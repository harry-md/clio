info:
  name: Delete Author
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{LOCAL_API_URL}}/authors/22"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
