info:
  name: Buy Subscription
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/orders/subscription
  body:
    type: json
    data: |-
      {
        "planId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
