info:
  name: Buy Books
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCAL_API_URL}}/orders"
  body:
    type: json
    data: |-
      {
        "bookIds": [27, 28]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
