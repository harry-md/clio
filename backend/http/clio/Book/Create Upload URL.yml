info:
  name: Create Upload URL
  type: http
  seq: 4

http:
  method: POST
  url: "{{LOCAL_API_URL}}/books/upload-url"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
