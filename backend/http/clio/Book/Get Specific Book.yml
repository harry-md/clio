info:
  name: Get Specific Book
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_API_URL}}/books/33"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
