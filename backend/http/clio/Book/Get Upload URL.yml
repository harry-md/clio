info:
  name: Get Upload URL
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/books/upload-url
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
