info:
  name: Admin Update Publisher
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/api/publishers/3
  body:
    type: json
    data: |-
      {
        "bankAccountNumber": 1231231231312311312311
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
