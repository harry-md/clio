info:
  name: Get Dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_API_URL}}/publishers/current-publisher/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
