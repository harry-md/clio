info:
  name: Get All Subscription Plan
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_API_URL}}/subscription-plans"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
