info:
  name: Get User Libraries
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_API_URL}}/libraries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
